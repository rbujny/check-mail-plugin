/**
 * Re-exports ProcessedEmailData as the canonical payload type
 * used for Chrome Message Passing between content scripts and
 * the service worker.
 */
export type { ProcessedEmailData } from '../types/email';
